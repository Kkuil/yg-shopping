<view class="goods_list">
    <!-- 搜索框 -->
    <Search />
    <!-- Tab栏切换 -->
    <Tab goods="{{goods}}" isLoading="{{isLoading}}" isReachBottom="{{isReachBottom}}" isEmpty="{{isEmpty}}" bindsortType="sortType"/>
</view>